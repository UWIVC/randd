[pytest]
addopts =
    # show summary of all tests that did not pass
    -ra
    # Make tracebackes shorter
    --tb=native
filterwarnings = 
    ignore::UserWarning
    ignore::DeprecationWarning
pythonpath = .
testpaths =
    test
xfail_strict = True